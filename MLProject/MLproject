# MLProject  ← simpan TANPA ekstensi
name: mlflow-salesforecasting

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "data_preprocessing/sales-data-preprocessed.csv"}
    command: >
      python modelling.py --data_path={data_path}
      #            ^-------------^   └── tanda '=' aman dari masalah spasi
